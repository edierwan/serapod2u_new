# Environment
NEXT_PUBLIC_APP_ENV=preprod

# Supabase (client) PREPROD
NEXT_PUBLIC_SUPABASE_URL=https://jqihlckqrhdxszgwuymu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<set in Vercel>

# Supabase (server — handle with care)
SUPABASE_SERVICE_ROLE_KEY=<set in Vercel>

# Postgres (pooler) — this is the one your app uses
DATABASE_POOL_URL=<set in Vercel>

# App URL for redirects
NEXT_PUBLIC_APP_URL=http://www.pre.serapod2u.com

CRON_SECRET=<set in Vercel>
