'use client'

export default function SerappReportsView() {
  return (
    <div className="space-y-3 px-4 py-6">
      <div className="serapp-card serapp-rise rounded-2xl p-4">
        <p className="serapp-eyebrow">Reports</p>
        <h1 className="mt-2 font-display text-lg font-semibold text-[var(--sera-ink)]">
          Distributor reports
        </h1>
        <p className="mt-2 text-sm leading-6 text-[var(--sera-muted)]">
          Lightweight Serapp reporting comes after the order + warehouse acceptance flow is stable.
        </p>
      </div>
    </div>
  )
}
