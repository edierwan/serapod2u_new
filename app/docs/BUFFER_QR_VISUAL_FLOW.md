# Buffer QR Code Generation Flow

## Visual Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    QR BATCH GENERATION REQUEST                   │
│  POST /api/qr-batches/generate { order_id }                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              1. Fetch Order with Order Items                     │
│  - Product variants (Cranberry Lychee, Keladi Cheese, etc.)    │
│  - Quantities per variant                                        │
│  - Buffer percent (10%)                                          │
│  - Manufacturer org code (e.g., "HM")                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              2. Call generateQRBatch()                           │
│  Parameters:                                                     │
│  - orderNo: "ORD-HM-1125-02"                                    │
│  - manufacturerCode: "HM"                                        │
│  - orderItems: [Variant A, Variant B, ...]                     │
│  - bufferPercent: 10                                             │
│  - unitsPerCase: 100                                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                  │
        ▼                                  ▼
┌──────────────────┐           ┌──────────────────────┐
│  VARIANT A       │           │  VARIANT B           │
│  Cranberry Lychee│           │  Keladi Cheese       │
│  ─────────────── │           │  ──────────────────  │
│  Normal: 3000    │           │  Normal: 2000        │
│  Buffer: 100     │           │  Buffer: 200         │
└────────┬─────────┘           └────────┬─────────────┘
         │                              │
         ▼                              ▼
┌────────────────────────────┐ ┌────────────────────────────┐
│  NORMAL CODE GENERATION    │ │  NORMAL CODE GENERATION    │
│  ────────────────────────  │ │  ────────────────────────  │
│  Seq: 1 - 3000             │ │  Seq: 3101 - 5100          │
│  is_buffer: false          │ │  is_buffer: false          │
│  case_number: 1-30         │ │  case_number: 31-50        │
│  variant_key: PROD-        │ │  variant_key: PROD-        │
│    CELVA9464-CRA-843412-HM │ │    CELVA9464-KEL-866575-HM │
│  status: printed           │ │  status: printed           │
└────────┬───────────────────┘ └────────┬───────────────────┘
         │                              │
         ▼                              ▼
┌────────────────────────────┐ ┌────────────────────────────┐
│  BUFFER CODE GENERATION    │ │  BUFFER CODE GENERATION    │
│  ────────────────────────  │ │  ────────────────────────  │
│  Seq: 3001 - 3100          │ │  Seq: 5101 - 5300          │
│  is_buffer: true           │ │  is_buffer: true           │
│  case_number: 0            │ │  case_number: 0            │
│  variant_key: PROD-        │ │  variant_key: PROD-        │
│    CELVA9464-CRA-843412-HM │ │    CELVA9464-KEL-866575-HM │
│  status: buffer_available  │ │  status: buffer_available  │
└────────┬───────────────────┘ └────────┬───────────────────┘
         │                              │
         └────────────┬─────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│              3. Insert into Database (qr_codes)                  │
│  - Batch insert with all fields populated                       │
│  - case_number, is_buffer, variant_key, status all set          │
└─────────────────────────────────────────────────────────────────┘
```

## Data Structure Comparison

### Before Implementation

```typescript
{
  sequence_number: 3001,
  code: "PROD-CELVA9464-CRA-843412-ORD-HM-1125-02-03001-...",
  status: "generated",
  // Missing: is_buffer, variant_key, case_number
}
```

### After Implementation

```typescript
// Normal Code
{
  sequence_number: 1500,
  code: "PROD-CELVA9464-CRA-843412-ORD-HM-1125-02-01500-...",
  is_buffer: false,
  case_number: 15,
  variant_key: "PROD-CELVA9464-CRA-843412-HM",
  status: "printed"
}

// Buffer Code
{
  sequence_number: 3001,
  code: "PROD-CELVA9464-CRA-843412-ORD-HM-1125-02-03001-...",
  is_buffer: true,
  case_number: 0,
  variant_key: "PROD-CELVA9464-CRA-843412-HM",
  status: "buffer_available"
}
```

## Mode C Reverse Job Integration

```
┌─────────────────────────────────────────────────────────────────┐
│              MODE C: Reverse Job Request                         │
│  User scans damaged codes from Case #15 (Cranberry Lychee)     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              1. Identify Damaged Codes                           │
│  - Extract variant_key from scanned codes                       │
│  - variant_key: "PROD-CELVA9464-CRA-843412-HM"                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              2. Query Buffer Pool for THIS Variant              │
│                                                                  │
│  SELECT * FROM qr_codes                                          │
│  WHERE variant_key = 'PROD-CELVA9464-CRA-843412-HM'            │
│    AND is_buffer = true                                          │
│    AND status = 'buffer_available'                              │
│  ORDER BY sequence_number                                        │
│                                                                  │
│  ✅ Returns: Seq 3001-3100 (Cranberry buffer codes only)       │
│  ❌ Won't return: Seq 5101-5300 (Keladi buffer codes)          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              3. Create Replacement Pairs                         │
│  Damaged Seq 1500 → Buffer Seq 3001                             │
│  Damaged Seq 1501 → Buffer Seq 3002                             │
│  (All same variant, no cross-contamination)                     │
└─────────────────────────────────────────────────────────────────┘
```

## Key Benefits Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                    BEFORE (Potential Issues)                     │
├─────────────────────────────────────────────────────────────────┤
│  🔴 No variant grouping                                          │
│     → Could use Keladi buffer for Cranberry damage              │
│                                                                  │
│  🔴 No buffer flag                                               │
│     → Hard to identify which codes are buffers                  │
│                                                                  │
│  🔴 No case assignment tracking                                  │
│     → Unclear which codes belong to which case                  │
│                                                                  │
│  🔴 Generic status                                               │
│     → Can't distinguish buffer availability                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    AFTER (Improvements)                          │
├─────────────────────────────────────────────────────────────────┤
│  ✅ Variant-specific buffer pools                                │
│     → Each variant has its own buffer codes                     │
│     → Query: WHERE variant_key = 'PROD-...-CRA-...-HM'         │
│                                                                  │
│  ✅ Clear buffer identification                                  │
│     → Query: WHERE is_buffer = true                             │
│     → Easy to count available buffers per variant               │
│                                                                  │
│  ✅ Case assignment tracking                                     │
│     → Normal codes: case_number = 1..N                          │
│     → Buffer codes: case_number = 0                             │
│                                                                  │
│  ✅ Status-based filtering                                       │
│     → buffer_available: Ready for use                           │
│     → printed: In normal pool                                   │
│     → spoiled: Marked as damaged                                │
└─────────────────────────────────────────────────────────────────┘
```

---

**Implementation Complete:** All new QR batches will have proper buffer flagging per variant! 🎉
