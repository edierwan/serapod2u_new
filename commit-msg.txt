revert(roadtour): remove Export to Accounting button from KPI report UI

- The incentive-export API requires a configured KPI cycle with actual
  incentive data to work properly
- Removed the button and all related code from the UI to avoid confusion
- The API endpoint /api/roadtour/kpi/incentive-export remains available
  for direct use when the data is ready