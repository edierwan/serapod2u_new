fix(roadtour): use report cycle ID directly for Export to Accounting button

- The previous implementation tried to fetch the cycle ID from a separate
  API call which failed due to date format mismatch
- Now uses report.cycle.id directly from the already-loaded report data
- This is more reliable and avoids an extra API call